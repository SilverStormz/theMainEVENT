 void runhuman(){
 humanY += humanSpeedY;

  if (humanY + humanHeight > floorY){
    humanY = floorY - humanHeight;
    humanSpeedY = 0
    
   Jumping = false;
  }
  else{
    humanSpeedY += 1;
  }
 
  rect(humanX, humanY, humanWidth, humanHeight );
 }