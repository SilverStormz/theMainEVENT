void restart()
{
   if(mouseX > 90 && mouseX < 595 && mouseY > 150 && mouseY < 29)
   {

     if(mousePressed)
     {
        vibe = height/2;
        for(int i = 0; i < pipeET.length; i++)
        {
          pipeET[i] = width + 200*i;
          pipeTE[i] = (int)random(-350, 0);
        }
        score = 0;
        gamePoint = 0;
     } 
   }
}